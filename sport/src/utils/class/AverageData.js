/**
* User Average Data
* @param {number} userId 
* @param {array} sessions 
*/
class AverageData{
    constructor(userId, sessions) {
        this.userId = userId
        this.sessions = sessions
    }
}
export default AverageData