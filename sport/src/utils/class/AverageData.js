    /**
     * User Average Data
     * @param {*} userId 
     * @param {*} sessions 
     */
class AverageData{
    constructor(userId, sessions) {
        this.userId = userId
        this.sessions = sessions
    }
}
export default AverageData